// ============================================================
// StyleGuru — Premium Feature Guard
// (Disabled - all features now free)
// ============================================================

function PremiumFeatureGuard({ children }) {
  // All features are now free, so we always show the children
  return children;
}

export default PremiumFeatureGuard;
